# Operating System
.DS_Store
Thumbs.db

# IDE and Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Dependencies
node_modules/
vendor/
venv/
env/
__pycache__/
*.pyc

# Build outputs
dist/
build/
out/
target/
*.log

# Environment and secrets
.env
.env.local
.env.*.local
*.key
*.pem

# Testing
coverage/
.coverage
*.cover
.pytest_cache/

# Package files
*.egg-info/
.eggs/

# Personal files
TASKS.md
MEMORY.md
.claude/worktrees/

# Cron and operational logs (content gitignored; directory tracked via .gitkeep)
logs/*.log
tmp/
.claude/settings.local.json

# News-digest runtime state (ledger + FactSet result cache — regenerated each run)
state/news_digest_seen.jsonl
state/factset_cache/
logs/news_digest_*.txt

# Chunk store (Store A) — embeddings + caches are regenerable via ingest.py.
# chunks.jsonl is small metadata but is a derived artifact too; regenerate, don't commit.
state/chunk_store/

# Obsidian per-device workspace state (don't sync between devices)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/workspace-tablet.json
.obsidian/cache
.obsidian/.cache

# Prevent future iPad/Obsidian noise from being committed
.obsidian/plugins/
.obsidian/community-plugins.json
.obsidian/graph.json
Untitled*.md
Untitled*.canvas
Untitled*.base

.obsidian/
