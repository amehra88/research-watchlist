# RIS4 portal build — NOT YET INSTALLED (paste into `crontab -e` when ready).
# 07:20 ET: after the 07:00 daily digest, before the 07:30 watchlist scraper.
# 19:20 ET (not 18:50): logs/news_digest.log postmarket "run done" times for
# the 7 days before this was written (09-09..09-15) ranged 18:36-19:15:43
# (latest: Mon 09-14); 19:20 clears that with margin. See docs/portal/README.md.

20 7 * * * cd /root/research-watchlist && /root/bin/alert_on_failure.sh portal_build python3 scripts/portal/build_portal.py >> logs/portal_build.log 2>&1
20 19 * * 1-5 cd /root/research-watchlist && /root/bin/alert_on_failure.sh portal_build python3 scripts/portal/build_portal.py >> logs/portal_build.log 2>&1

# RIS5 A3: daily FactSet valuation snapshot (prices/market_value/consensus
# SALES+EPS+EBITDA+FCF, amendment v1.2).
# 04:45 ET weekdays (fix round 1, coordinator review — NOT 05:45): `crontab -l` shows
# etf_flows_fetch runs 05:30 ET weekdays with ~15 claude -p calls of its own, and
# run_daily.sh's own comment records that two claude -p-heavy jobs sharing one rolling 5h
# window exhausted the subscription on 2026-08-13 (the news digest shipped 40/256
# stories). Nothing else is scheduled 04:30-05:30 ET (checked the full crontab). This
# job's own calls (2 prices + up to 8 market_value at the 25-id cap, fix 2, + 2 calls each
# for 4 consensus metrics = up to 18, plus any 408/5xx retries — see task-3-report.md's
# "Live run" section for the actual count) run well clear before 05:30 at 04:45. Writes
# state/valuation/{prices,consensus}_<date>.jsonl (gitignored) + latest.json (tracked).
45 4 * * 1-5 cd /root/research-watchlist && set -a && . /root/podcasts/.env && set +a && /root/bin/alert_on_failure.sh valuation_snapshot python3 scripts/valuation/snapshot.py >> logs/valuation_snapshot.log 2>&1

# RIS5 A3 (amendment v1.2): weekly FY4-FY5 consensus (SALES/EPS/EBITDA/FCF, with counts),
# merged into the LATEST existing latest.json (Friday's, since the daily snapshot itself
# is weekdays-only, 1-5) via `snapshot.py --weekly` — run_weekly() refuses if no
# latest.json exists at all, but does not require it to be from the same calendar day.
# Sunday 09:00 ET, after the 08:30 Fundamentals pull (below in this file) so the two
# FactSet-heavy Sunday jobs don't overlap. NOT run live yet — code path implemented and unit-tested
# (fix 2) but the coordinator's instruction was code-only for this amendment, no live
# FY4-FY5 pull today.
0 9 * * 0 cd /root/research-watchlist && set -a && . /root/podcasts/.env && set +a && /root/bin/alert_on_failure.sh valuation_weekly_consensus python3 scripts/valuation/snapshot.py --weekly >> logs/valuation_weekly_consensus.log 2>&1

# RIS5 A3: weekly FactSet Fundamentals (net debt/total debt/cash at QTR;
# gross/operating margin + FCF + sales + derived FCF margin at LTM, RIS5 A3 fix 4).
# Sunday, after the Store B weekly refresh (08:00) so both FactSet-heavy jobs never
# overlap. `fundamentals.py`'s `main()` now fully orchestrates the pull (RIS5 A3
# fix 5): builds the universe from identity, makes the TWO required
# FactSet_Fundamentals calls (BALANCE_SHEET_METRICS at QTR, FLOW_METRICS at LTM -- a
# single LTM call against the balance-sheet metrics returns 0% coverage, discovered
# live during fix 4, hence the split), retries a 408/5xx once per batch, aborts
# cleanly on a session-limit/429 without writing a partial file, derives fcf_margin
# from the same row via compute_fcf_margin_rows(), and writes
# state/valuation/fundamentals_<date>.jsonl idempotently. `--dry-run` (see
# `python3 scripts/valuation/fundamentals.py --dry-run`) prints the planned calls
# without spawning claude -p or writing anything, for a quick sanity check before
# actually installing this line. NOT YET installed (still "paste into crontab -e
# when ready", same as the rest of this file) -- code-complete and tested, an
# operator decision away from going live, not blocked on any remaining gap.
30 8 * * 0 cd /root/research-watchlist && set -a && . /root/podcasts/.env && set +a && /root/bin/alert_on_failure.sh valuation_fundamentals python3 scripts/valuation/fundamentals.py >> logs/valuation_fundamentals.log 2>&1

# RIS5 A3: daily archive of the ws ETF-holdings-change report (peer-ETF trim/exit
# history) — state/etf_trades/<date>.json (gitignored). Independent of build_portal
# (see etf_trades_archive.py's module docstring for why); run any time after ws's own
# daily 07:30 scrape has produced today's report.
0 8 * * * cd /root/research-watchlist && /root/bin/alert_on_failure.sh etf_trades_archive python3 scripts/portal/etf_trades_archive.py >> logs/etf_trades_archive.log 2>&1

# RIS5 A3: peer-ETF trim clusters -> challenge evidence on competitive-advantage
# assumptions (reads the archive above; writes state/thesis/evidence_log.jsonl +
# _thesis.md pressure, same pipeline as insider_pull.py). After the archive above.
15 8 * * * cd /root/research-watchlist && set -a && . /root/podcasts/.env && set +a && /root/bin/alert_on_failure.sh etf_evidence python3 scripts/thesis/etf_evidence.py >> logs/etf_evidence.log 2>&1
